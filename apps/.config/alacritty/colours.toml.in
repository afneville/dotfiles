[colors.bright]
black = "#${COL3}"
blue = "#${COL13}"
cyan = "#${COL12}"
green = "#${COL11}"
magenta = "#${COL14}"
red = "#${COL8}"
white = "#${COL5}"
yellow = "#${COL10}"

[colors.normal]
black = "#${COL0}"
blue = "#${COL13}"
cyan = "#${COL12}"
green = "#${COL11}"
magenta = "#${COL14}"
red = "#${COL8}"
white = "#${COL5}"
yellow = "#${COL10}"

[colors.cursor]
cursor = "#${COL5}"
text = "#${COL0}"

[colors.primary]
background = "#${COL0}"
foreground = "#${COL5}"
